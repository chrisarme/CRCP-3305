// Similiar to "import" in java. This "imports" the iostream 
#include <iostream>

// This uses the namespace std (Standard Library) and allows for shortened calls
using namespace std;

// This is the starting point of the program. Everything branches out from here
int main()
{
	// cout = console output
	// This line outputs "Hello, World!" with a new line afterwads into the console output
	cout << "Hello, World!\n";

	// Sends a return code letting the user know everything is OK
	return 0;
}